# ============================================================
# Dépendances — ASD Detection API (MobileNetV2 / FastAPI)
# ============================================================
# Installation :
#   pip install -r requirements.txt --break-system-packages
#   (ou dans un venv : pip install -r requirements.txt)
# ============================================================

# ── API Web ──────────────────────────────────────────────────
fastapi==0.115.5          # Framework API REST asynchrone
uvicorn[standard]==0.32.1 # Serveur ASGI (production-ready avec watchfiles)
python-multipart==0.0.17  # Nécessaire pour UploadFile / Form dans FastAPI

# ── Deep Learning (CPU uniquement) ───────────────────────────
# tensorflow-cpu évite d'installer les bibliothèques CUDA/cuDNN inutiles.
# Choisir la version compatible avec votre Python :
#   Python 3.9–3.12 → tensorflow-cpu 2.16.x
tensorflow-cpu==2.20.0

# ── Traitement d'images ──────────────────────────────────────
Pillow==11.0.0            # Décodage et manipulation d'images (PIL)
numpy==2.1.3              # Calcul numérique (compatible tf 2.20)

# ── Modèles de données / validation ──────────────────────────
pydantic==2.10.3          # Schémas de requête/réponse (inclus avec FastAPI 0.115)
opencv-python==4.13.0.92
mediapipe==0.10.35
seaborn==0.13.2